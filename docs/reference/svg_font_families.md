# Detect font families referenced by an SVG

Detect font families referenced by an SVG

## Usage

``` r
svg_font_families(svg_content)
```

## Arguments

- svg_content:

  SVG string.

## Value

Unique character vector of font family names (generics removed).
