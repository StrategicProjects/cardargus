# Convert CSS gradient to SVG gradient

Convert CSS gradient to SVG gradient

## Usage

``` r
css_gradient_to_svg(css_gradient, id = "grad")
```

## Arguments

- css_gradient:

  CSS gradient string

- id:

  ID for the gradient

## Value

SVG gradient definition
