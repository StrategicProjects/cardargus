# Extract the leading number from a dimension string

Extract the leading number from a dimension string

## Usage

``` r
num_lead(x)
```

## Arguments

- x:

  A character such as `"500"`, `"40px"`, `"100%"`, or `NA`.

## Value

Numeric value, or `NA_real_` if none is found.
